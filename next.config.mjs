/** @type {import('next').NextConfig} */
const nextConfig = {
  // Real Next.js server on Vercel (not a static export), so API routes and
  // live server-side fetching work.
  trailingSlash: true,
}

export default nextConfig
