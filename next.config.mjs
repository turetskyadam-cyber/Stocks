/** @type {import('next').NextConfig} */
const nextConfig = {
  // Running as a real Next.js server on Vercel (not a static export), so that
  // API routes and live server-side data fetching work.
  trailingSlash: true,
}

export default nextConfig
